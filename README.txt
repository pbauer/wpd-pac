

This is a demo of plone.app.widgets, plone.app.toolbar and plone.app.contenttypes
based on the starzel standard buildout.

To use:

    ln -s local_develop.cfg local.cfg
    cp secret.cfg_tmpl secret.cfg

